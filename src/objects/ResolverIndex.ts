//import { createGetterResolver, createLookupResolver } from '../extensions/type-graphql';
export { DevelopResolver } from './Develop/developResolver';

// export const DocumentResolver = createGetterResolver(Document);
